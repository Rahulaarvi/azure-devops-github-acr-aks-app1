apiVersion: v1
kind: Service
metadata:
    name: dockerbuildpushanddeploytoaks
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: dockerbuildpushanddeploytoaks